{
  "name": "skill-harbor",
  "version": "0.2.0",
  "private": true,
  "description": "SkillHarbor — marketplace for Cursor agent skills, rules, commands, and subagents",
  "scripts": {
    "dev": "concurrently -n api,ui -c blue,green \"npm run dev:api\" \"npm run dev:ui\"",
    "dev:api": "cd backend && python -m uvicorn main:app --reload --host 127.0.0.1 --port 8765",
    "dev:ui": "cd frontend && npm run dev",
    "install:all": "cd frontend && npm install && cd ../backend && pip install -r requirements.txt",
    "build:ui": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
